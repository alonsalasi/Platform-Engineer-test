apiVersion: v1
kind: ConfigMap
metadata:
  name: ecs-manager-code
  namespace: ecs-platform
data:
  server.py: |
    from http.server import HTTPServer, BaseHTTPRequestHandler
    import json
    import os
    import requests
    
    GITHUB_TOKEN = os.environ.get('GITHUB_TOKEN')
    GITHUB_REPO = os.environ.get('GITHUB_REPO')
    
    class ECSManagerHandler(BaseHTTPRequestHandler):
        def do_GET(self):
            if self.path == '/':
                self.send_response(200)
                self.send_header('Content-type', 'text/html')
                self.end_headers()
                with open('/app/index.html', 'rb') as f:
                    self.wfile.write(f.read())
            elif self.path == '/health':
                self.send_response(200)
                self.send_header('Content-type', 'application/json')
                self.end_headers()
                self.wfile.write(json.dumps({'status': 'healthy'}).encode())
            elif self.path == '/api/ecs/list':
                import boto3
                try:
                    ecs = boto3.client('ecs', region_name='us-east-1')
                    response = ecs.list_clusters()
                    clusters = []
                    for arn in response.get('clusterArns', []):
                        name = arn.split('/')[-1]
                        desc = ecs.describe_clusters(clusters=[arn])['clusters'][0]
                        clusters.append({
                            'name': name,
                            'status': desc['status'],
                            'runningTasks': desc['runningTasksCount'],
                            'pendingTasks': desc['pendingTasksCount']
                        })
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({'clusters': clusters}).encode())
                except Exception as e:
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({'error': str(e)}).encode())
            elif self.path == '/api/asg/list':
                import boto3
                try:
                    asg = boto3.client('autoscaling', region_name='us-east-1')
                    response = asg.describe_auto_scaling_groups()
                    groups = []
                    for group in response.get('AutoScalingGroups', []):
                        groups.append({
                            'name': group['AutoScalingGroupName'],
                            'minSize': group['MinSize'],
                            'maxSize': group['MaxSize'],
                            'desiredCapacity': group['DesiredCapacity'],
                            'instances': len(group['Instances'])
                        })
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({'asgs': groups}).encode())
                except Exception as e:
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({'error': str(e)}).encode())
            elif self.path.startswith('/api/asg/status/'):
                import boto3
                asg_name = self.path.split('/')[-1]
                try:
                    asg = boto3.client('autoscaling', region_name='us-east-1')
                    response = asg.describe_auto_scaling_groups(AutoScalingGroupNames=[asg_name])
                    if response['AutoScalingGroups']:
                        group = response['AutoScalingGroups'][0]
                        self.send_response(200)
                        self.send_header('Content-type', 'application/json')
                        self.end_headers()
                        self.wfile.write(json.dumps({
                            'status': 'active',
                            'instances': len(group['Instances']),
                            'desired': group['DesiredCapacity']
                        }).encode())
                    else:
                        self.send_response(200)
                        self.send_header('Content-type', 'application/json')
                        self.end_headers()
                        self.wfile.write(json.dumps({'status': 'creating'}).encode())
                except Exception as e:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({'status': 'creating'}).encode())
            else:
                self.send_response(404)
                self.end_headers()
        
        def do_POST(self):
            if self.path == '/api/asg/create':
                content_length = int(self.headers['Content-Length'])
                body = self.rfile.read(content_length)
                data = json.loads(body.decode())
                
                webhook_url = f'https://api.github.com/repos/{GITHUB_REPO}/dispatches'
                payload = {
                    'event_type': 'create-asg',
                    'client_payload': {
                        'asgName': data.get('asgName'),
                        'instanceType': data.get('instanceType'),
                        'minSize': data.get('minSize'),
                        'maxSize': data.get('maxSize'),
                        'desiredCapacity': data.get('desiredCapacity'),
                        'clusterName': data.get('clusterName'),
                        'targetCapacity': data.get('targetCapacity', '80'),
                        'maxScalingStep': data.get('maxScalingStep', '10'),
                        'minScalingStep': data.get('minScalingStep', '1'),
                        'terminationProtection': data.get('terminationProtection', 'DISABLED')
                    }
                }
                
                response = requests.post(
                    webhook_url,
                    headers={
                        'Authorization': f'token {GITHUB_TOKEN}',
                        'Accept': 'application/vnd.github.v3+json'
                    },
                    json=payload
                )
                
                if response.status_code == 204:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'message': 'ASG creation triggered',
                        'status': 'pending'
                    }).encode())
                else:
                    print(f'GitHub API Error: {response.status_code} - {response.text}')
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'error': f'GitHub API {response.status_code}: {response.text}'
                    }).encode())
            
            elif self.path == '/api/asg/delete':
                content_length = int(self.headers['Content-Length'])
                body = self.rfile.read(content_length)
                data = json.loads(body.decode())
                
                webhook_url = f'https://api.github.com/repos/{GITHUB_REPO}/dispatches'
                payload = {
                    'event_type': 'delete-asg',
                    'client_payload': {
                        'asgName': data.get('asgName'),
                        'user': 'k8s-platform'
                    }
                }
                
                response = requests.post(
                    webhook_url,
                    headers={
                        'Authorization': f'token {GITHUB_TOKEN}',
                        'Accept': 'application/vnd.github.v3+json'
                    },
                    json=payload
                )
                
                if response.status_code == 204:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'message': 'ASG deletion triggered',
                        'status': 'pending'
                    }).encode())
                else:
                    print(f'GitHub API Error: {response.status_code} - {response.text}')
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'error': f'GitHub API {response.status_code}: {response.text}'
                    }).encode())
            
            elif self.path == '/api/ecs/create':
                content_length = int(self.headers['Content-Length'])
                body = self.rfile.read(content_length)
                data = json.loads(body.decode())
                
                # Trigger GitHub Actions
                webhook_url = f'https://api.github.com/repos/{GITHUB_REPO}/dispatches'
                payload = {
                    'event_type': 'create-ecs-cluster',
                    'client_payload': {
                        'clusterName': data.get('clusterName'),
                        'capacityProvider': data.get('capacityProvider', 'FARGATE'),
                        'containerInsights': data.get('containerInsights', 'disabled'),
                        'asgName': data.get('asgName', ''),
                        'targetCapacity': data.get('targetCapacity', '80'),
                        'maxScalingStep': data.get('maxScalingStep', '10'),
                        'minScalingStep': data.get('minScalingStep', '1'),
                        'terminationProtection': data.get('terminationProtection', 'DISABLED')
                    }
                }
                
                response = requests.post(
                    webhook_url,
                    headers={
                        'Authorization': f'token {GITHUB_TOKEN}',
                        'Accept': 'application/vnd.github.v3+json'
                    },
                    json=payload
                )
                
                if response.status_code == 204:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'message': 'GitHub Actions triggered',
                        'status': 'pending'
                    }).encode())
                else:
                    print(f'GitHub API Error: {response.status_code} - {response.text}')
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'error': f'GitHub API {response.status_code}: {response.text}'
                    }).encode())
            
            elif self.path == '/api/ecs/update':
                content_length = int(self.headers['Content-Length'])
                body = self.rfile.read(content_length)
                data = json.loads(body.decode())
                
                # Trigger GitHub Actions
                webhook_url = f'https://api.github.com/repos/{GITHUB_REPO}/dispatches'
                payload = {
                    'event_type': 'update-ecs-cluster',
                    'client_payload': {
                        'clusterName': data.get('clusterName'),
                        'containerInsights': data.get('containerInsights'),
                        'user': 'k8s-platform'
                    }
                }
                
                response = requests.post(
                    webhook_url,
                    headers={
                        'Authorization': f'token {GITHUB_TOKEN}',
                        'Accept': 'application/vnd.github.v3+json'
                    },
                    json=payload
                )
                
                if response.status_code == 204:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'message': 'GitHub Actions triggered',
                        'status': 'pending'
                    }).encode())
                else:
                    print(f'GitHub API Error: {response.status_code} - {response.text}')
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'error': f'GitHub API {response.status_code}: {response.text}'
                    }).encode())
            
            elif self.path == '/api/ecs/delete':
                content_length = int(self.headers['Content-Length'])
                body = self.rfile.read(content_length)
                data = json.loads(body.decode())
                
                # Trigger GitHub Actions
                webhook_url = f'https://api.github.com/repos/{GITHUB_REPO}/dispatches'
                payload = {
                    'event_type': 'delete-ecs-cluster',
                    'client_payload': {
                        'clusterName': data.get('clusterName'),
                        'user': 'k8s-platform'
                    }
                }
                
                response = requests.post(
                    webhook_url,
                    headers={
                        'Authorization': f'token {GITHUB_TOKEN}',
                        'Accept': 'application/vnd.github.v3+json'
                    },
                    json=payload
                )
                
                if response.status_code == 204:
                    self.send_response(200)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'message': 'GitHub Actions triggered',
                        'status': 'pending'
                    }).encode())
                else:
                    self.send_response(500)
                    self.send_header('Content-type', 'application/json')
                    self.end_headers()
                    self.wfile.write(json.dumps({
                        'error': 'Failed to trigger GitHub Actions'
                    }).encode())
            else:
                self.send_response(404)
                self.end_headers()
    
    if __name__ == '__main__':
        server = HTTPServer(('0.0.0.0', 8080), ECSManagerHandler)
        print('ECS Manager running on port 8080...')
        server.serve_forever()
  
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>ECS Platform Manager</title>
        <style>
            * { margin: 0; padding: 0; box-sizing: border-box; }
            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                padding: 20px;
            }
            .container {
                max-width: 1200px;
                margin: 0 auto;
            }
            .header {
                background: rgba(255, 255, 255, 0.95);
                backdrop-filter: blur(10px);
                border-radius: 20px;
                padding: 30px;
                margin-bottom: 30px;
                box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                display: flex;
                justify-content: space-between;
                align-items: center;
            }
            .header h1 {
                font-size: 2.5em;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                display: flex;
                align-items: center;
                gap: 15px;
            }
            .badge {
                background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
                color: white;
                padding: 8px 16px;
                border-radius: 20px;
                font-size: 0.9em;
                font-weight: 600;
                box-shadow: 0 4px 15px rgba(17, 153, 142, 0.4);
            }
            .card {
                background: rgba(255, 255, 255, 0.95);
                backdrop-filter: blur(10px);
                border-radius: 20px;
                padding: 30px;
                margin-bottom: 30px;
                box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                transition: transform 0.3s ease, box-shadow 0.3s ease;
            }
            .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 25px 70px rgba(0,0,0,0.4);
            }
            .card h2 {
                color: #333;
                margin-bottom: 20px;
                font-size: 1.8em;
                display: flex;
                align-items: center;
                gap: 10px;
            }
            .form-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 20px;
                margin-bottom: 20px;
            }
            .form-group {
                display: flex;
                flex-direction: column;
            }
            label {
                color: #555;
                font-weight: 600;
                margin-bottom: 8px;
                font-size: 0.9em;
                text-transform: uppercase;
                letter-spacing: 0.5px;
            }
            input, select {
                padding: 12px 16px;
                border: 2px solid #e0e0e0;
                border-radius: 10px;
                font-size: 1em;
                transition: all 0.3s ease;
                background: white;
            }
            input:focus, select:focus {
                outline: none;
                border-color: #667eea;
                box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
            }
            .btn {
                padding: 14px 28px;
                border: none;
                border-radius: 10px;
                font-size: 1em;
                font-weight: 600;
                cursor: pointer;
                transition: all 0.3s ease;
                text-transform: uppercase;
                letter-spacing: 0.5px;
                box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            }
            .btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(0,0,0,0.3);
            }
            .btn-primary {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
            }
            .btn-refresh {
                background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
                color: white;
            }
            .btn-edit {
                background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
                color: white;
                padding: 8px 16px;
                font-size: 0.85em;
            }
            .btn-delete {
                background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
                color: white;
                padding: 8px 16px;
                font-size: 0.85em;
            }
            .alert {
                padding: 20px 24px;
                border-radius: 15px;
                margin-bottom: 15px;
                font-weight: 600;
                font-size: 1.1em;
                animation: slideInBounce 0.5s ease;
                box-shadow: 0 8px 30px rgba(0,0,0,0.3);
                border: 3px solid rgba(255,255,255,0.3);
                backdrop-filter: blur(10px);
            }
            @keyframes slideInBounce {
                0% { transform: translateX(400px) scale(0.5); opacity: 0; }
                60% { transform: translateX(-20px) scale(1.05); opacity: 1; }
                100% { transform: translateX(0) scale(1); opacity: 1; }
            }
            .alert-success {
                background: linear-gradient(135deg, #00b894 0%, #00ff88 100%);
                color: white;
                text-shadow: 0 2px 4px rgba(0,0,0,0.2);
            }
            .alert-error {
                background: linear-gradient(135deg, #ff3838 0%, #ff6b6b 100%);
                color: white;
                text-shadow: 0 2px 4px rgba(0,0,0,0.2);
            }
            table {
                width: 100%;
                border-collapse: separate;
                border-spacing: 0;
                margin-top: 20px;
            }
            th {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                padding: 16px;
                text-align: left;
                font-weight: 600;
                text-transform: uppercase;
                font-size: 0.85em;
                letter-spacing: 0.5px;
            }
            th:first-child { border-radius: 10px 0 0 0; }
            th:last-child { border-radius: 0 10px 0 0; }
            td {
                padding: 16px;
                border-bottom: 1px solid #f0f0f0;
                background: white;
            }
            tr:last-child td:first-child { border-radius: 0 0 0 10px; }
            tr:last-child td:last-child { border-radius: 0 0 10px 0; }
            tr:hover td {
                background: #f8f9ff;
            }
            .actions {
                display: flex;
                gap: 10px;
            }
            .empty-state {
                text-align: center;
                padding: 60px 20px;
                color: #999;
                font-size: 1.2em;
            }
            .loading {
                display: inline-block;
                width: 20px;
                height: 20px;
                border: 3px solid rgba(255,255,255,0.3);
                border-radius: 50%;
                border-top-color: white;
                animation: spin 1s ease-in-out infinite;
            }
            @keyframes spin {
                to { transform: rotate(360deg); }
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">
                <h1>🚀 ECS Platform Manager</h1>
                <span class="badge">Kubernetes IDP</span>
            </div>
            
            <div id="alerts" style="position:fixed; top:20px; right:20px; z-index:2000; max-width:400px;"></div>
            
            <div class="card">
                <h2>📊 Active Clusters</h2>
                <button onclick="loadClusters()" class="btn btn-refresh">🔄 Refresh</button>
                <div id="clustersList"></div>
            </div>
            
            <div class="card">
                <h2>➕ Create ECS Cluster</h2>
                <div class="form-grid">
                    <div class="form-group">
                        <label>Cluster Name</label>
                        <input type="text" id="clusterName" placeholder="my-production-cluster">
                    </div>
                    <div class="form-group">
                        <label>Capacity Provider</label>
                        <select id="capacityProvider" onchange="toggleASGSection()">
                            <option value="FARGATE">AWS Fargate</option>
                            <option value="FARGATE_SPOT">Fargate Spot</option>
                            <option value="EC2">EC2 Instances</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>Container Insights</label>
                        <select id="containerInsights">
                            <option value="disabled">Disabled (Free)</option>
                            <option value="enabled">Enabled (Paid)</option>
                        </select>
                    </div>
                </div>
                
                <div id="asgSection" style="display:none; margin-top:20px;">
                    <div class="form-group">
                        <label>Select Auto Scaling Group</label>
                        <select id="asgChoice" style="width:100%;">
                            <option value="">Loading ASGs...</option>
                        </select>
                        <button onclick="loadASGs()" class="btn btn-refresh" style="margin-top:10px; padding:8px 16px; font-size:0.9em;">🔄 Refresh ASG List</button>
                    </div>
                </div>
                
                <button onclick="createCluster()" class="btn btn-primary" style="margin-top:20px;">🚀 Deploy Cluster via GitOps</button>
            </div>
            
            <div class="card">
                <h2>🖥️ Manage Auto Scaling Groups</h2>
                <p style="color:#666; margin-bottom:20px;">Create or delete ASGs for EC2-based ECS clusters.</p>
                
                <div style="margin-bottom:30px;">
                    <h3 style="color:#333; font-size:1.2em; margin-bottom:10px;">Delete Existing ASG</h3>
                    <div style="display:flex; gap:10px; align-items:flex-end;">
                        <div class="form-group" style="flex:1;">
                            <label>Select ASG to Delete</label>
                            <select id="asgDeleteChoice" style="width:100%;">
                                <option value="">Loading ASGs...</option>
                            </select>
                        </div>
                        <button onclick="deleteASG()" class="btn btn-delete" style="padding:12px 24px;">🗑️ Delete ASG</button>
                    </div>
                </div>
                
                <h3 style="color:#333; font-size:1.2em; margin-bottom:15px;">Create New ASG</h3>
                
                <div class="form-grid">
                    <div class="form-group">
                        <label>ASG Name</label>
                        <input type="text" id="asgName" placeholder="my-ecs-asg">
                    </div>
                    <div class="form-group">
                        <label>Instance Type</label>
                        <select id="instanceType">
                            <option value="t3.micro">t3.micro (2 vCPU, 1GB RAM)</option>
                            <option value="t3.small">t3.small (2 vCPU, 2GB RAM)</option>
                            <option value="t3.medium">t3.medium (2 vCPU, 4GB RAM)</option>
                            <option value="t3.large">t3.large (2 vCPU, 8GB RAM)</option>
                        </select>
                    </div>
                </div>
                
                <div class="form-grid" style="margin-top:10px;">
                    <div class="form-group">
                        <label>Min Size</label>
                        <input type="number" id="minSize" value="1" min="0" max="10">
                    </div>
                    <div class="form-group">
                        <label>Max Size</label>
                        <input type="number" id="maxSize" value="3" min="1" max="10">
                    </div>
                    <div class="form-group">
                        <label>Desired Capacity</label>
                        <input type="number" id="desiredCapacity" value="2" min="0" max="10">
                    </div>
                </div>
                
                <h3 style="color:#333; font-size:1.1em; margin:25px 0 15px 0; padding-top:20px; border-top:2px solid #e0e0e0;">Capacity Provider Settings</h3>
                <p style="color:#666; font-size:0.9em; margin-bottom:15px;">Configure auto-scaling behavior for ECS tasks</p>
                
                <div class="form-grid">
                    <div class="form-group">
                        <label>Target Capacity (%)</label>
                        <input type="number" id="targetCapacity" value="80" min="1" max="100">
                        <small style="color:#999; font-size:0.85em;">Keep instances at this % utilization</small>
                    </div>
                    <div class="form-group">
                        <label>Max Scaling Step</label>
                        <input type="number" id="maxScalingStep" value="10" min="1" max="100">
                        <small style="color:#999; font-size:0.85em;">Max instances to add at once</small>
                    </div>
                    <div class="form-group">
                        <label>Min Scaling Step</label>
                        <input type="number" id="minScalingStep" value="1" min="1" max="10">
                        <small style="color:#999; font-size:0.85em;">Min instances to add at once</small>
                    </div>
                </div>
                
                <div class="form-group" style="margin-top:10px;">
                    <label>Termination Protection</label>
                    <select id="terminationProtection">
                        <option value="DISABLED">Disabled (Allow scale-down)</option>
                        <option value="ENABLED">Enabled (Prevent termination)</option>
                    </select>
                </div>
                
                <button onclick="createASG()" class="btn btn-primary" style="margin-top:20px;">🚀 Create ASG via GitOps</button>
                
                <div id="asgProgress" style="display:none; margin-top:20px; padding:20px; background:linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius:10px; color:white;">
                    <h4 style="margin-bottom:10px;">⏳ Creating Auto Scaling Group...</h4>
                    <div id="asgStatus" style="font-size:1.1em; font-weight:500;"></div>
                    <div style="margin-top:10px; font-size:0.9em; opacity:0.9;">🕒 This may take 2-5 minutes. Progress updates every 5 seconds.</div>
                </div>
            </div>
        </div>
        
        <div id="editModal" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.7); z-index:1000; align-items:center; justify-content:center;">
            <div style="background:white; border-radius:20px; padding:40px; max-width:500px; width:90%; box-shadow:0 20px 60px rgba(0,0,0,0.5);">
                <h2 style="margin-bottom:20px; color:#333;">✏️ Edit Cluster Settings</h2>
                <p style="color:#666; margin-bottom:20px;">Cluster: <strong id="editClusterName"></strong></p>
                <div class="form-group">
                    <label>Container Insights</label>
                    <select id="editInsights" style="width:100%;">
                        <option value="disabled">Disabled (Free)</option>
                        <option value="enabled">Enabled (Paid)</option>
                    </select>
                </div>
                <div style="display:flex; gap:10px; margin-top:30px;">
                    <button onclick="applyEdit()" class="btn btn-primary" style="flex:1;">✔️ Apply Changes</button>
                    <button onclick="closeEditModal()" class="btn" style="flex:1; background:#e0e0e0; color:#333;">❌ Cancel</button>
                </div>
            </div>
        </div>
        
        <script>
            async function loadClusters() {
                const list = document.getElementById('clustersList');
                list.innerHTML = '<div class="empty-state"><div class="loading"></div> Refreshing clusters...</div>';
                
                try {
                    const response = await fetch('/api/ecs/list');
                    const data = await response.json();
                    
                    if (data.clusters && data.clusters.length > 0) {
                        list.innerHTML = '<table><thead><tr><th>Cluster Name</th><th>Status</th><th>Running Tasks</th><th>Pending Tasks</th><th>Actions</th></tr></thead><tbody>' +
                            data.clusters.map(c => `<tr><td><strong>${c.name}</strong></td><td>${c.status}</td><td>${c.runningTasks}</td><td>${c.pendingTasks}</td><td><div class="actions"><button onclick="editCluster('${c.name}')" class="btn btn-edit">✏️ Edit</button><button onclick="deleteCluster('${c.name}')" class="btn btn-delete">🗑️ Delete</button></div></td></tr>`).join('') +
                            '</tbody></table>';
                    } else {
                        list.innerHTML = '<div class="empty-state">📦 No clusters found. Create your first cluster below!</div>';
                    }
                } catch (error) {
                    list.innerHTML = '<p style="color:red;">Error loading clusters</p>';
                }
            }
            
            window.onload = () => {
                loadClusters();
                loadASGs();
            };
            
            async function loadASGs() {
                try {
                    const response = await fetch('/api/asg/list');
                    const data = await response.json();
                    const select = document.getElementById('asgChoice');
                    const deleteSelect = document.getElementById('asgDeleteChoice');
                    
                    if (data.asgs && data.asgs.length > 0) {
                        const options = data.asgs.map(a => `<option value="${a.name}">${a.name} (${a.instances} instances)</option>`).join('');
                        select.innerHTML = '<option value="">-- Select Existing ASG --</option>' + options;
                        deleteSelect.innerHTML = '<option value="">-- Select ASG to Delete --</option>' + options;
                    } else {
                        select.innerHTML = '<option value="">No ASGs available - Create one below</option>';
                        deleteSelect.innerHTML = '<option value="">No ASGs available</option>';
                    }
                    
                } catch (error) {
                    const select = document.getElementById('asgChoice');
                    const deleteSelect = document.getElementById('asgDeleteChoice');
                    select.innerHTML = '<option value="">⚠️ Error loading ASGs</option>';
                    deleteSelect.innerHTML = '<option value="">⚠️ Error loading ASGs</option>';
                }
            }
            
            function toggleASGSection() {
                const provider = document.getElementById('capacityProvider').value;
                const section = document.getElementById('asgSection');
                
                if (provider === 'EC2') {
                    section.style.display = 'block';
                    loadASGs();
                } else {
                    section.style.display = 'none';
                }
            }
            
            async function createASG() {
                const asgName = document.getElementById('asgName').value;
                const instanceType = document.getElementById('instanceType').value;
                const minSize = document.getElementById('minSize').value;
                const maxSize = document.getElementById('maxSize').value;
                const desiredCapacity = document.getElementById('desiredCapacity').value;
                const targetCapacity = document.getElementById('targetCapacity').value;
                const maxScalingStep = document.getElementById('maxScalingStep').value;
                const minScalingStep = document.getElementById('minScalingStep').value;
                const terminationProtection = document.getElementById('terminationProtection').value;
                const clusterName = 'temp-cluster';
                
                if (!asgName) {
                    showAlert('Please enter ASG name', 'error');
                    return;
                }
                
                try {
                    const response = await fetch('/api/asg/create', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify({
                            asgName, instanceType, minSize, maxSize, desiredCapacity, clusterName,
                            targetCapacity, maxScalingStep, minScalingStep, terminationProtection
                        })
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        showAlert('ASG creation started! Tracking progress...', 'success');
                        document.getElementById('asgProgress').style.display = 'block';
                        trackASGCreation(asgName);
                    } else {
                        showAlert('Error: ' + data.error, 'error');
                    }
                } catch (error) {
                    showAlert('Error: ' + error.message, 'error');
                }
            }
            
            async function trackASGCreation(asgName) {
                const statusDiv = document.getElementById('asgStatus');
                let attempts = 0;
                const maxAttempts = 60;
                let lastCheck = Date.now();
                
                statusDiv.innerHTML = '⏳ Step 1/4: Triggering GitHub Actions workflow...';
                
                const interval = setInterval(async () => {
                    attempts++;
                    
                    try {
                        const response = await fetch(`/api/asg/status/${asgName}`);
                        const data = await response.json();
                        
                        if (data.status === 'active') {
                            statusDiv.innerHTML = `✅ ASG Created Successfully!<br>Instances: ${data.instances}/${data.desired} running`;
                            clearInterval(interval);
                            showAlert(`ASG "${asgName}" is ready! Refreshing list...`, 'success');
                            setTimeout(() => {
                                loadASGs();
                                document.getElementById('asgProgress').style.display = 'none';
                                document.getElementById('asgName').value = '';
                            }, 2000);
                        } else {
                            const step = attempts < 10 ? '2/4: Building terraform config...' : 
                                       attempts < 30 ? '3/4: Creating AWS resources...' : 
                                       '4/4: Launching EC2 instances...';
                            statusDiv.innerHTML = `⏳ ${step}<br>Progress: ${attempts}/${maxAttempts} checks`;
                        }
                        
                        // Check if workflow failed (no ASG after 2 minutes)
                        if (attempts > 24 && data.status === 'creating') {
                            statusDiv.innerHTML = '❌ ASG Creation Failed!<br>Check <a href="https://github.com/alonsalasi/Platform-Engineer-test/actions" target="_blank" style="color:white; text-decoration:underline;">GitHub Actions</a> for error details.';
                            clearInterval(interval);
                            showAlert('ASG creation failed. Check GitHub Actions logs.', 'error');
                            document.getElementById('asgProgress').style.display = 'none';
                        }
                        
                        if (attempts >= maxAttempts) {
                            statusDiv.innerHTML = '⚠️ Timeout reached. Check <a href="https://github.com/alonsalasi/Platform-Engineer-test/actions" target="_blank" style="color:white; text-decoration:underline;">GitHub Actions</a> for status.';
                            clearInterval(interval);
                            showAlert('ASG creation timeout. Check GitHub Actions.', 'error');
                        }
                    } catch (error) {
                        statusDiv.innerHTML = `⏳ Waiting for ASG creation...<br>Check ${attempts}/${maxAttempts}`;
                    }
                }, 5000);
            }
            
            function editCluster(clusterName) {
                document.getElementById('editClusterName').textContent = clusterName;
                document.getElementById('editModal').style.display = 'flex';
            }
            
            function closeEditModal() {
                document.getElementById('editModal').style.display = 'none';
            }
            
            async function applyEdit() {
                const clusterName = document.getElementById('editClusterName').textContent;
                const insights = document.getElementById('editInsights').value;
                
                closeEditModal();
                
                try {
                    const response = await fetch('/api/ecs/update', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify({clusterName, containerInsights: insights})
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        showAlert('Update triggered! Check GitHub for progress', 'success');
                        setTimeout(loadClusters, 3000);
                    } else {
                        showAlert('Error: ' + data.error, 'error');
                    }
                } catch (error) {
                    showAlert('Failed to update cluster', 'error');
                }
            }
            
            function showAlert(message, type) {
                const alerts = document.getElementById('alerts');
                const alert = document.createElement('div');
                alert.className = `alert alert-${type}`;
                alert.textContent = message;
                alerts.appendChild(alert);
                setTimeout(() => alert.remove(), 5000);
            }
            
            async function createCluster() {
                const clusterName = document.getElementById('clusterName').value;
                const capacityProvider = document.getElementById('capacityProvider').value;
                const containerInsights = document.getElementById('containerInsights').value;
                
                if (!clusterName) {
                    showAlert('Please enter a cluster name', 'error');
                    return;
                }
                
                let payload = {clusterName, capacityProvider, containerInsights};
                
                if (capacityProvider === 'EC2') {
                    const asgChoice = document.getElementById('asgChoice').value;
                    if (!asgChoice) {
                        showAlert('Please create or select an ASG before creating EC2 cluster', 'error');
                        return;
                    }
                    payload.asgName = asgChoice;
                    payload.targetCapacity = '80';
                    payload.maxScalingStep = '10';
                    payload.minScalingStep = '1';
                    payload.terminationProtection = 'DISABLED';
                }
                
                try {
                    const response = await fetch('/api/ecs/create', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify(payload)
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        showAlert('GitHub Actions triggered! Check GitHub for progress (2-5 min)', 'success');
                        document.getElementById('clusterName').value = '';
                        setTimeout(loadClusters, 3000);
                    } else {
                        showAlert('Error: ' + data.error, 'error');
                    }
                } catch (error) {
                    showAlert('Error: ' + error.message, 'error');
                }
            }
            
            async function deleteASG() {
                const asgName = document.getElementById('asgDeleteChoice').value;
                
                if (!asgName) {
                    showAlert('Please select an ASG to delete', 'error');
                    return;
                }
                
                if (!confirm(`Are you sure you want to delete ASG "${asgName}"?\n\nThis will terminate all EC2 instances in the group.\n\nThis action cannot be undone.`)) {
                    return;
                }
                
                try {
                    const response = await fetch('/api/asg/delete', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify({asgName})
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        showAlert('ASG deletion triggered! Check GitHub for progress', 'success');
                        setTimeout(loadASGs, 3000);
                    } else {
                        showAlert('Error: ' + data.error, 'error');
                    }
                } catch (error) {
                    showAlert('Error: ' + error.message, 'error');
                }
            }
            
            async function deleteCluster(clusterName) {
                if (!confirm(`Are you sure you want to delete cluster "${clusterName}"?\n\nThis action cannot be undone.`)) {
                    return;
                }
                
                try {
                    const response = await fetch('/api/ecs/delete', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify({clusterName})
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        showAlert('Cluster deletion triggered! Check GitHub for progress', 'success');
                        setTimeout(loadClusters, 3000);
                    } else {
                        showAlert('Error: ' + data.error, 'error');
                    }
                } catch (error) {
                    showAlert('Error: ' + error.message, 'error');
                }
            }
        </script>
    </body>
    </html>
